#!/usr/bin/env node

'use strict';

/**
 * Parse Commands
 */
const program = require('commander');
program
    .usage('[options]')
    .parse(process.argv);

process.env.NODE_ENV = 'development';
global.visionConfig = require('../src/getVisionConfig');

const dev = require('../src/dev');
dev(require('../config/vision'));
