#!/usr/bin/env node

'use strict';

/**
 * Parse Commands
 */
const program = require('commander');
program
    .usage('[options]')
    .parse(process.argv);

process.env.NODE_ENV = 'production';
global.visionConfig = require('../src/getVisionConfig');

const build = require('../src/build');
build(require('../config/vision'));
